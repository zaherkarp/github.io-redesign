# Source for the "currently" rows in the psql closer at the bottom of
# index.html. Injected at build time by scripts/build_portfolio.py
# between <!-- now-block:start --> ... <!-- now-block:end --> markers.
#
# Style: lowercase prose for values (matches the `status` row register),
# even for book titles and product names. Use `·` (middot) for inline
# lists; do not use em dashes.
#
# Empty values are skipped — leave a key blank to suppress that row
# without removing it from the schema.

reads:  designing data-intensive applications
music:  ""
watch:  ""
learn:  obsidian
build:  ""
based:  ""
